<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Campgrounds</title>
</head>
<body>
    <h1>Edit Campground</h1>
    <form action="/campgrounds/<%=campground._id%>?_method=PUT" method = "POST"><%# using _method from method override%>
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="campground[title]" value="<%=campground.title%>"> <%# campground[title] is a way to group things together so everytime our data is sent via post request, to our express app to the server, everthing in the req.body will be stored under campground %>
        </div>
        <div>
            <label for="location">Location</label>
            <%# value attribute pre populates the fields with the current data about the campground %>
            <input type="text" id="location" name="campground[location]" value="<%=campground.location%>">
        </div>
        <button>Update Campground</button>
    </form>
    <a href="/campgrounds/<%= campground._id%>">Back to Campgrounds</a>
    
</body>
</html>